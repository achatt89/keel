<!--
  Keel template — docs/adr/README.md (the ADR index)
  WHAT: The index of every decision record, plus the register of open questions that are NOT
        yet decisions. One row per ADR; one row per open QUESTION (not per marker location).
  INCLUDE WHEN: Product tier and up. Even a prototype benefits from 3–5 ADRs on its hard bets.
  OWNS: The `ADR-xxx` namespace. IDs are reserved here (by the phase script at scope time)
        before a task worktree ever writes one. Never renumber; supersede.
  LAYOUT: one file per record — `docs/adr/ADR-xxx-<slug>.md` (see `ADR-NNN-slug.md` template).
        Two ADRs never share a file, so parallel worktrees never conflict here.
  IMMUTABILITY: Accepted ADRs are immutable and carry NO addenda. Lifecycle only moves forward:
        Proposed → Accepted → Built → ⚠️ Superseded. Deploy state → CHANGELOG / DEPLOYMENT.
        Incidents → RUNBOOK Postmortems. (conventions.md "ADR lifecycle")
  Delete all <!-- Keel guidance --> comments when filling this in.
-->

# Architecture Decision Records — {{PROJECT_NAME}}

All decisions derive from constraints and requirements in `BRD.md`; ADRs record technical
reasoning, they do not relitigate product choices. One file per decision; this file is the index.

> Lifecycle: `Proposed` → `Accepted` → `Built` → `⚠️ Superseded by ADR-xxx`. No addenda — a
> deviation that doesn't fit the one `Build deviation` row is a new ADR.

## Index

<!-- Keel guidance: one row per ADR in number order. Title is "Topic — Choice". Status carries
     the lifecycle stage with the date it was reached; the file's own header table holds the
     commits. Deciders: founder / engineering / legal (one or more). Reserved-but-unwritten IDs
     (allocated by a phase script at scope time) appear as `Reserved (Phase N)`. -->

| ADR | Title | Status | Deciders | Date |
|---|---|---|---|---|
| [ADR-001](./ADR-001-{{slug}}.md) | {{Topic}} — {{Chosen option}} | Built | engineering | {{YYYY-MM-DD}} |
| [ADR-002](./ADR-002-{{slug}}.md) | {{Topic}} — {{Chosen option}} | Accepted | founder · engineering | {{YYYY-MM-DD}} |
| [ADR-003](./ADR-003-{{slug}}.md) | {{Topic}} — {{Chosen option}} | ⚠️ Superseded by ADR-00X | engineering | {{YYYY-MM-DD}} |
| ADR-004 | Reserved (Phase {{N}}) — {{planned topic}} | Proposed | — | — |

## Open decisions

<!-- Keel guidance: every `[NEEDS DECISION]` marker anywhere in the suite maps to exactly ONE
     row here — one row per QUESTION, listing every location that restates it. Decider tag says
     who can close it. Closing a row = a human decided → write the ADR, remove every marker,
     delete the row. NEVER invent a decision to close a row. -->

| # | Question | Locations | Decider | Options surfaced | Status |
|---|---|---|---|---|---|
| OD-01 | {{the question, once}} | `docs/COMPLIANCE.md` §{{n}} · `docs/RUNBOOK.md` §{{n}} | legal | {{a / b}} | Open |
| OD-02 | {{the question, once}} | `docs/IMPLEMENTATION_PLAN.md` Phase {{N}} | founder | {{a / b / decline}} | Open |

## Unclassified (review)

<!-- Keel guidance: only ever populated by `/keel upgrade` FORMAT_MIGRATION when a legacy ADR
     addendum could not be confidently classified as build-deviation / deploy-state / incident /
     feedback. Each row quotes the source verbatim with its origin. A human resolves and deletes
     the row. Leave this section out of a fresh generation. -->

*(none)*

---

*Index of `docs/adr/` — {{PROJECT_NAME}}. Regenerated by doc-sync; hand edits are fine.*
