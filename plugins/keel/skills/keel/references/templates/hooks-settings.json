{
  "_comment": "Generated by Keel. MERGE this hook entry into any existing .claude/settings.json — never overwrite the whole file. Remove this _comment key before writing. Impeccable's own hook is NOT wired here — it is activated separately by running `/impeccable hooks on`, which writes .claude/settings.local.json (impeccable's own convention). Writing it here too would double-fire the hook.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/modern-web-guidance-hook.mjs"
          }
        ]
      }
    ]
  }
}
